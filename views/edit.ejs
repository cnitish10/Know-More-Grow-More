<%- include('partials/header') -%>

<div class="ui main text container segment">
    <div class="ui huge header">Update your Current status <%=blog.title%>

        
        <form class="ui form" action="/blogs/<%= blog._id%>?_method=PUT" method="POST">
          
                <div class='field'> <input type="text" name="blog[title]" value="<%= blog.title%>"></div>
                <div class='field'><input type="text" name="blog[company]" value="<%= blog.company%>"></div>
                <div class='field'><input type="text" name="blog[contact]" value="<%= blog.contact%>"></div>
                <div class="field"><textarea name="blog[body]" cols="30" rows="10" placeholder="share your Experience"><%= blog.body%></textarea></div>
                <button class="ui violet basic button">submit</button>
            
            </form>
        </div>
</div>


<%- include('partials/footer') -%>